# BudgetAir MCP server runtime ONLY. Installed in the Dockerfile.
# Pinned to the tested versions; all have prebuilt wheels for Python 3.12.
# (Dashboard deps -> ../requirements.txt ; QA/eval deps -> ../requirements-dev.txt)
fastmcp==3.4.4
mcp==1.28.1
pandas==2.2.0
numpy==1.26.4
plotly==6.6.0
kaleido==1.3.0
pyarrow==23.0.1
