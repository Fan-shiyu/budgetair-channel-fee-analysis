# Streamlit dashboard runtime ONLY (app.py / pages / utils.py / core.py).
# MCP server deps -> mcp_server/requirements.txt ; QA/eval deps -> requirements-dev.txt
# Pinned to the tested versions; all have prebuilt wheels for Python 3.12 (.python-version).
streamlit==1.55.0
pandas==2.2.0
numpy==1.26.4
plotly==6.6.0
