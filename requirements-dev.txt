# Local QA + eval tooling ONLY — NOT required at runtime by the dashboard or the
# MCP server. Installs both runtimes plus the dev tools, on Python 3.12.
#   pytest        -> tests/test_tools.py
#   playwright    -> qa_dashboard.py (browser QA of the dashboard)
#   google-genai  -> eval/golden_eval.py (Gemini golden-question eval)
-r requirements.txt
-r mcp_server/requirements.txt
pytest==8.0.0
playwright==1.61.0
google-genai==2.11.0
